{
  "name": "utils",
  "version": "1.0.0",
  "description": "Websockify has been forked out into its own project.  `launch.sh` wil automatically download it here if it is not already present and not installed as system-wide.",
  "main": "genkeysymdef.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-core": "^6.26.3",
    "browserify": "^16.5.0",
    "commander": "^5.0.0",
    "fs-extra": "^9.0.0"
  }
}
